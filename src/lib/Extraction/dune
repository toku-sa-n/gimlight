(library
 (name gimlight)
 (libraries controller zarith))

(coq.extraction
 (prelude Extraction)
 (extracted_modules
  BinNat
  EventHandler
  FixedSizeArray
  GameInitializationUsecase
  IO
  Main
  Map
  MapStore
  MapRepository)
 (theories Controller Coquill GameLogic RAMStore Repository Usecase))

(env
 (dev
  (flags
   (:standard -w -a))))
